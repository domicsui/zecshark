# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Authentication Secret for Admin Portal
JWT_SECRET=zeckshark_jwt_secret_key_change_in_production_2026

# Optional: Twitter/X API v2 Bearer Token (for production verification mode)
# If empty, DEMO/MOCK verification engine is used
X_BEARER_TOKEN=
